import React, { useState, useEffect } from 'react';

function xxx() {
    const [state, setstate] = useState(initialState)
    useEffect(() => {
        return () => {
        }
    }, [])
    return (
        <div>
        </div>
    );
}

export default xxx

